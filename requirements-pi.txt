# ============================================================
#  LettuVault — Raspberry Pi Requirements
#  Target: Raspberry Pi 4/5, 64-bit OS (aarch64)
#  Python: 3.10+
# ============================================================
#
#  ⚠️  DO NOT use the root requirements.txt on the Pi.
#     That file contains Windows-only wheels for torch, torchvision,
#     and the GUI-dependent opencv-python.
#
#  INSTALL ORDER ON THE PI:
#  ─────────────────────────────────────────────────────────────
#  Step 1 — Install PyTorch ARM wheel first (not on PyPI):
#
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
#  Step 2 — Install everything else from this file:
#
#    pip install -r requirements-pi.txt
#
#  Step 3 — Install the workspace packages (editable):
#
#    pip install -e .
# ============================================================

# --- CORE BACKEND & WEB ---
fastapi==0.129.0
uvicorn==0.40.0
websockets==15.0.1
python-multipart==0.0.22
starlette==0.52.1
pydantic==2.12.5
pydantic-settings==2.9.0
pydantic_core==2.27.2
python-dotenv==1.2.2

# --- DATABASE & ORM ---
SQLAlchemy==2.0.36
alembic==1.13.1

# --- SECURITY & AUTH ---
python-jose==3.3.0
passlib==1.7.4
cryptography==44.0.0
rsa==4.9
ecdsa==0.19.0
pyasn1==0.6.1

# --- MQTT PROTOCOLS ---
paho-mqtt==2.1.0
amqtt==0.11.0

# --- AI & VISION ---
# torch + torchvision: installed separately via ARM wheel (see INSTALL ORDER above)
# DO NOT add them here — PyPI wheels are x86-only.
ultralytics==8.3.0
opencv-python-headless==4.10.0.84   # ← headless build: no GUI/X11 required
numpy==1.26.4
scipy==1.13.1
pillow==10.4.0
matplotlib==3.9.2
PyYAML==6.0.2

# --- DATA PROCESSING & UTILS ---
polars==0.20.31
# polars-runtime-32 omitted — Windows-only package, not available on ARM
networkx==3.3
requests==2.32.3
urllib3==2.2.3
certifi==2024.8.30
charset-normalizer==3.4.0
idna==3.10
six==1.16.0
cycler==0.12.1
contourpy==1.3.0
kiwisolver==1.4.7
fonttools==4.53.1
pyparsing==3.1.4
python-dateutil==2.9.0.post0
typing_extensions==4.12.2
fsspec==2024.9.0

# --- SYSTEM & CLI ---
psutil==6.0.0
rich==13.9.4
click==8.1.7
typer==0.12.5
Jinja2==3.1.4
MarkupSafe==3.0.1
Mako==1.3.5
filelock==3.16.1
anyio==4.6.2.post1
exceptiongroup==1.2.4
greenlet==3.1.1
h11==0.14.0
cffi==1.17.1
pycparser==2.22
transitions==0.9.2
dacite==1.8.1
annotated-types==0.7.0
typing-inspection==0.4.0
shellingham==1.5.4
markdown-it-py==3.0.0
mdurl==0.1.2
Pygments==2.18.0

# --- YOUR LOCAL WORKSPACE ---
-e .
